@model List<Feature>
@{
    ViewData["Title"] = "FeatureList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h3>Öne Çıkanlar</h3>

<table class="table table-bordered">
    <
    <tr>
        <th>#</th>
        <th>İsim Soyisim</th>
        <th>1. Başlık</th>
        <th>2. Başlık</th>
        <th>3. Başlık</th>
        <th>Resim Url</th>
        <th>Güncelle</th>
        <th>Sil</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.FeatureId</td>
            <td>@item.FeatureNameSurname</td>
            <td>@item.FeatureTitle1</td>
            <td>@item.FeatureTitle2</td>
            <td>@item.FeatureTitle3</td>
            <td>@item.FeatureImageURL</td>
            <td><a href="/Feature/UpdateFeature/@item.FeatureId" class="btn btn-outline-success">Güncelle</a></td>
            <td><a href="/Feature/DeleteFeature/@item.FeatureId" class="btn btn-outline-danger">Sil</a></td>
        </tr>
    }

</table>
<br />
<td><a href="/Feature/CreateFeature/" class="btn btn-outline-primary col-md-12">Yeni Öne Çıkan Ekle</a></td>